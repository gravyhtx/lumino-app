/**
 * Color themes enum.
 * @enum {string} themeNames
 * @property {string} DEFAULT - Default theme
 * @property {string} LIGHT - Light theme
 * @property {string} DARK - Dark theme
 * @readonly
 */
export const themeNames = [
  'DEFAULT',
  'LIGHT',
  'DARK',
] as const